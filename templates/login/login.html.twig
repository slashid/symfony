{% extends 'base.html.twig' %}

{% block body %}

<slashid-form
    {% for attributeName,attributeValue in configuration %}
        {% if attributeName is int %}
            {{ attributeValue }}
        {% else %}
            {{ attributeName }}="{{ attributeValue }}"
        {% endif %}
    {% endfor %}
></slashid-form>

{#
<script>
    SlashIdSettings = {
        loginCallbackUrl: "{{ $loginCallbackUrl }}",
        csfrToken: "{{ $csrfToken }}"
    };
</script>

@if ($useGlue)
    <script src="{{ asset('vendor/slashid/slashid.laravel-web-login.js') }}"></script>
@endif

@if ($useBundled)
    <script type="module" src="{{ asset('vendor/slashid/bundled-form/main.js') }}"></script>
@endif
#}

{% endblock %}
